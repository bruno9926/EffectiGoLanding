---
interface Props {
  title: string;
  value: string | number;
}

const { title, value }: Props = Astro.props;
---

<div class="flex flex-col items-center justify-center gap-2 text-center">
    <span class="text-4xl font-semibold">{value}</span>
    <span class="text-xl">{title}</span>
</div>