---
const { title, description, image, imageAlt = "Ícono", info } = Astro.props;
---

<div class="relative max-w-[420px] mx-auto my-4">
  <div
    class="absolute inset-0 w-80 translate-x-3 -translate-y-3 rounded-3xl bg-white border border-[#dddddd] z-0"
  >
  </div>
  <div
    class="absolute inset-0 w-80 translate-x-1.5 -translate-y-1.5 rounded-3xl bg-white border border-[#dddddd] z-10"
  >
  </div>
  <div
    class="relative w-80 h-98 z-20 rounded-3xl bg-white border border-[#dddddd] p-8 pt-10 pb-10"
  >
    <div class="w-20 h-20 mb-6">
      <img src={image} alt={imageAlt} />
    </div>
    <h2 class="text-2xl font-bold mb-4 w-40">{title}</h2>
    <p class="text-base text-[#222]">{description}</p>
    {info && <p class="text-base text-[#222] mt-3 text-[12px]">{info}</p>}
  </div>
</div>
